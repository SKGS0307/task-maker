<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form px-10 py-10 text-white">
            <form action="/create" method="post">
                <input class="block w-full rounded-md outline-none px-5 py-3 bg-zinc-800" type="text" placeholder="Tittles goes here" name="tittle">
                <textarea class="block w-full rounded-md outline-none px-5 py-3 mt-2 bg-zinc-800 resize-none" placeholder="write your task here" name="details"></textarea>
                <input class="mt-2 bg-blue-600 rounded-md px-5 py-2" type="submit" value="Create Task">
            </form>
        
        </div>
        
        <div class="tasks p-10 flex gap-3 flex-wrap">

            <% if(files.length > 0){%>
               
                <% files.forEach(function(val){ %>
                
                        <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md ">
                            <h1 class="text-white text-3xl tracking-tighter"> <%= val %></h1>
                            <a class="text-blue-600 inline-block mt-3" href="/file/<%= val%>">Read more..</a>
                        </div>

                    <% })%>
                
                <% } else { %>
                    
                        <h3 class="text-zinc-600">No task created yet</h3>
                    
                    <% }%>

            

            
            
            
        </div>
    </div>
</body>
</html>